{
  "name": "smtp-pop-container-tests",
  "version": "1.0.0",
  "description": "SMTP/POP server testing environment",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test:smtp": "tsx src/test-smtp.ts",
    "test:pop": "tsx src/test-pop.ts",
    "send:mail": "tsx src/send-test-mail.ts",
    "test:flow": "tsx src/test-flow.ts",
    "test:all": "npm run test:smtp && npm run test:pop && npm run send:mail && npm run test:flow"
  },
  "keywords": ["smtp", "pop3", "mail", "testing"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "nodemailer": "^6.9.0",
    "@types/nodemailer": "^6.4.0"
  }
}
